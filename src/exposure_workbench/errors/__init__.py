"""Failure vocabulary — what a run says when it stops."""

from exposure_workbench.errors.workflow_codes import (  # noqa: F401
    CODES,
    BriefNotSubmitted,
    RunRefused,
    classify,
    detail_of,
    speaks_for_itself,
)
